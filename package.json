{
  "name": "bankist",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sass" : "sass sass/style.sass css/style.css --watch"
  },
  "devDependencies": {
    "vite": "^2.8.0"
  }
}
